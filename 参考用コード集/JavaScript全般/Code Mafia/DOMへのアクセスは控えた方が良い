

 - - - - -
 
 for(let c of str) {
    el.innerHTML = `<span>${c}</span>`;
 }
 
  - - - - -

上の記述は、 el という要素のDOMにアクセスして、文字列 （エレメント） を追加していくという作業を、
" 配列str "の文字数分おこなうというプログラムだが、
その度にDOMへの参照・変更をおこなっているので、非常にパフォーマンスの悪いプログラムとなっている



