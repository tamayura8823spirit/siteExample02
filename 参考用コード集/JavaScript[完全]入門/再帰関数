

// 再帰関数
function recursion(n, max) {
  // 引数nをコンソールに出力
  console.log(n);

  // 引数を２倍にする
  n *= 2;

  // nがmax を越えたら終了
  if (n > max) { return; }

  // 再帰的に呼び出す
  recursion(n , max);
}

// 初期の値を１６、最大値を１０００として実行
recursion(16, 1000);



// 結果
16
32
64
128
256
512

