

* コンテンツの表示を" スクロール "と連動させるためには、ウィンドウのサイズや、スクロール位置を得る必要がある
  また、プログラムからスクロールをおこなうときもある


 - - - - -


[ コンテンツ領域のサイズやスクロール位置を得るプロパティ ]
.innerWidth      |    Webページのコンテンツ領域の幅    垂直スクロールバーの幅を含む
.innerHeight     |    Webページのコンテンツ領域の高さ    水平スクロールバーの高さを含む
.scrollX         |    水平にスクロールされているピクセル数
.scrollY         |    垂直にスクロールされているピクセル数


[ スクロールをおこなうメソッド ]
.scrollTo(x , y)    |   絶対位置スクロール    x , y の位置までスクロール
.scrollTo(x , y)    |   相対位置スクロール    x , y の量だけスクロール



 - - - - -


• 例文

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      width: 5000px;
      height: 10000px;
    }

    #inf {
      position: fixed;
      top: 0;
      background: #fcc;
      padding: 0.5em -1em;
    }
  </style>
  <title>Document</title>
</head>
<body>

  <pre id="inf"></pre>
  
  <script>
    // 繰り返し処理
    const intervalID = setInterval(() => {
      // 相対位置スクロール
      scrollBy(50, 100);

      // #inf に情報を表示
      viewInf();
    }, 100);

    // 一定時間後に停止
    setTimeout(() => {
      // 絶対位置スクロール
      scrollTo(0 , 0);

      // #inf に情報を表示
      viewInf();

      // 繰り返し処理を停止
      clearInterval(intervalID);
    }, 5000);

    // #inf に情報を表示
    function viewInf() {
      // 表示用の文字列を作成
      const t = `
      scrollX : ${scrollX}
      scrollY : ${scrollY}
      innerWidth : ${innerWidth}
      innerHeight: ${innerHeight}
      `;

      // idが inf の要素に文字列を設定
      document.querySelector('#inf').innerText = t.trim();
    }
  </script>
  
</body>
</html>



// 解説
0.1 秒ごとに、右に 50 ピクセル、下に 100 ピクセルずつ移動するプログラム
そして 5　秒後に左上端（0 , 0）の位置に戻す
また、 scrollX 、 scrollY 、 innerHeight の情報をWebページの左上に表示する



